# OCUPA Backend

Minimal Spring Boot backend scaffold for OCUPA.

Run:

```bash
cd ocupa-backend
mvn spring-boot:run
```

Configure `src/main/resources/application.properties` with your MySQL credentials.
